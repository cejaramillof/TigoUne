// Preserve minimum height of main divfunction setMainHeight() {    document.getElementById("ttr-main").style.minHeight = ( window.innerHeight - document.getElementById("ttr-main").offsetTop ) + "px";    if (document.getElementById("ttr-single-productos")) document.getElementById("ttr-single-productos").style.height = document.getElementById("ttr-main").offsetHeight + "px";}document.addEventListener('DOMContentLoaded', setMainHeight);window.addEventListener('resize', setMainHeight);// Side Menu Function(function ($) {    // Define variables    var frame = $(window);    var body = $('body');    var main = $('#ttr-main');    var navicon = $('#ttr-navicon');    var sideBar = $('#ttr-sidebar-left');    var overlay = $('#ttr-screen-overlay');    var openClass = "ttr-open-sidemenu";    var currentScroll = frame.scrollTop(); // Get current scroll position    var open = 1; // Set sidemenu to open state    var offseTop = main.offset().top; // Get position on main in y-Axis    toggleSideMenu(); // Initialize sidemenu on closed state    function toggleSideMenu() {        if (open == 0) {            // If sidemenu is closed then open it.            navicon.addClass(openClass);            sideBar.addClass(openClass);            overlay.addClass(openClass);            currentScroll = frame.scrollTop(); //Get current scroll position            body.css({'position': 'fixed', 'width': '100%'}); // Reset scroll on body            if (main.height() > frame.height()) body.css({'overflow-y': 'scroll'}); // If main div is taller than window, show inactive scrollbar            main.css('top', -currentScroll + offseTop); // Set main to last scroll position            open = 1; // Set sidemenu to open state        } else {            // If sidemenu is opened then close it.            navicon.removeClass(openClass);            sideBar.removeClass(openClass);            overlay.removeClass(openClass);            body.css({'position': 'static', 'overflow-y': 'auto', 'width': '100%'}); // Reset body attributes            main.css('top', offseTop + 'px'); // Reset main div attributes            frame.scrollTop(currentScroll); // Preserve position of scroll            setTimeout(function () {                sideBar.scrollTop(0)            }, 500); // Reset scroll position of sidemenu after closed            open = 0; // Set sidemenu to closed state        }    }    $(document).on('click', '.ttr-toggle-sidemenu', function (e) {        e.preventDefault(); // Prevent behavior of links        e.stopPropagation(); // Stop bubbling        toggleSideMenu();        // If element clicked is a link, close sidemenu and redirect        var href = $(this).attr('href');        if (href) {            setTimeout(function () {                window.location = href            }, 500);        }    });})(jQuery);(function ( $ ) {    $('#ttt-header-menu-button').on('click', function(){        toggle_header_menu();    });    function toggle_header_menu(){        $('#ttt-main-header-nav').toggleClass('closed');        $('#ttt-header-menu-button').toggleClass('closed');    }    $(document).on('click', function (e){        var navElement = $('#ttt-main-header-nav');        var navButton = $('#ttt-header-menu-button');        if (!navButton.is(e.target) && navButton.has(e.target).length === 0) {            setTimeout(function(){                navElement.addClass('closed');                navButton.addClass('closed');            },100);        }    });})( jQuery );/*Home Tabs*/(function ( $ ) {    $(document).ready(function() {        $('.tabs .tab-links a').on('click', function(e)  {            var currentAttrValue = $(this).attr('href');            // Show/Hide Tabs            $('.tabs ' + currentAttrValue).show().siblings().hide();            // Change/remove current tab to active            $(this).parent('li').addClass('active').siblings().removeClass('active');            e.preventDefault();        });    });})( jQuery );